# HormuzWatch — Code Remediation Plan & Implementation Map

**Lead Systems Architect & Remediation Engineer**  
**Baseline:** Adversarial Technical Audit Report (`docs/ML/ADVERSARIAL_TECHNICAL_AUDIT_REPORT.md`)  
**Standard:** Strict Technical Defensibility & Reproducible Empirical Verification  

---

## 1. Phase 0: Repository Discovery & Implementation Map

| Subsystem / Component | Repository Location | Current Implementation | Upstream / Downstream Dependencies | Existing Tests | Existing Benchmarks | Implementation Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Kinematics & Geodesy Engine** | `server/internal/geo/haversine.go`, `landmask.go`, `attack.go` | Great-circle Haversine with spherical radius $R=3440.065$ NM; 0.05° rasterized land-mask ray-caster. | Ingestion workers (`aisstream`, `opensky`) → State Manager | None | None | **PARTIALLY CORRECT** (Requires spherical disclaimer, Geodesic interface, Land-mask semantic reclassification) |
| **Track State & Adaptive EWMA** | `server/internal/intelligence/state.go`, `features.go` | In-memory track cache; pseudo "RMS Z-score" dividing by $\mu$ instead of $\sigma$; shortest-arc heading delta. | Telemetry Ingestion → State Manager → Feature Extraction | None | None | **INCORRECT** (Must implement true standardized running residual $\sigma^2_t$, $Z_t$, and separate COG from Heading) |
| **Rule-Based Anomaly Scorer** | `server/internal/anomaly/scorer.go`, `geofence.go` | Heuristic scoring (0–100) assigning points for AIS gap, speed drops, and geofence proximity. | Feature Vector → Composite Scorer | None | None | **MISLEADING** (Equated AIS gaps directly to hostile intent; needs non-hostile anomaly reframing) |
| **ML Inference Service (Python)** | `service/ml-service/lib/scoring.py`, `lib/features.py`, `grpc_server.py`, `app.py` | 6 domain ensembles using Isolation Forest + LOF with fixed 0.55/0.45 weights + Isotonic Calibration; TreeSHAP on IF. | Go MLClient (gRPC :8091 / HTTP :8090) | `tests/test_ml_service.py` (basic) | None | **PARTIALLY CORRECT** (Score blending misnamed as stacking; SHAP scope overclaimed; synthetic dataset) |
| **ML Training & Dataset Pipeline** | `service/ml-service/lib/training.py`, `api/train_all_models.py` | Parametric synthetic distribution generation with non-overlapping distributions; no temporal/vessel splits. | Offline training CLI → Models directory | None | None | **UNSUPPORTED** (Synthetic dataset must be explicitly labeled synthetic; manifest required) |
| **Go Pipeline & Ingestion Queue** | `server/internal/intelligence/pipeline.go`, `composite.go` | Bounded Go channel `chan *Observation` (cap 5000) with 20 worker goroutines and drop-tail backpressure. | Integrations (`aisstream`, `opensky`, `simulator`) → Worker Pool → ML / DB / WS | None | None | **PARTIALLY CORRECT** (Described as lock-free ring buffer; must be bounded channel queue with backpressure) |
| **gRPC Client & Circuit Breaker** | `server/internal/intelligence/ml_client.go` | 3-state Circuit Breaker (`CLOSED`, `OPEN`, `HALF-OPEN`); unary gRPC calls; in-memory 30s cache. | Pipeline → Python gRPC Server | None | None | **PARTIALLY CORRECT** (Uses deprecated dial options; silent fallback needs SRE observability) |
| **Database & Persistence** | `server/internal/db/db.go`, `telemetry.go`, `queries.go` | PostgreSQL / Supabase via `pgx/v5` with simple protocol (`prefer_simple_protocol=true`). | Ingestion Pipeline → PostgreSQL | None | None | **PARTIALLY CORRECT** (RCA on single-row syncs corrected to index lock contention & batching) |
| **WebSocket Hub & Streaming** | `server/internal/websocket/hub/hub.go`, `server/internal/api/handlers.go` | Gorilla WebSocket hub with per-client send channels (cap 256) and non-blocking broadcast drops. | Pipeline / Handlers → Browser Clients | None | None | **PLAUSIBLE** (Capacity target; requires slow-client safety verification and load testing) |
| **Observability & SRE CLI** | `server/internal/observability/metrics.go`, `service/sre/main.go`, `sre.sh` | Slog structured logging; atomic Prometheus metrics (`/metrics`); Go SRE CLI with tolerance test. | Prometheus / Grafana / Operator | `observability/logging_test.go` | SRE load benchmark | **LOCAL VALIDATION** (Needs queue metrics, gRPC/DB latency histograms, health probe decoupling) |
| **Geospatial Pub/Sub Architecture** | `docs/ARCHITECTURE.md`, `server/internal/heatmap/aggregator.go` | In-memory 0.05° grid binning for heatmap; H3/Quadtree documented conceptually. | Telemetry Ingestion → Heatmap Grid | None | None | **ARCHITECTURAL PROPOSAL** (Needs explicit role delineation: H3 for broker routing, R-Tree for viewports) |

---

## 2. Comprehensive Remediation Matrix (Items 1 – 26)

| # | Remediation Item | Target Files | Remediation Description | Verification / Test Plan |
| :- | :--- | :--- | :--- | :--- |
| **1** | **Kinematics Engine** | `server/internal/geo/haversine.go`, `server/internal/domain/telemetry/telemetry.go`, `server/internal/integrations/aisstream.go` | 1. Explicitly document Haversine as spherical great-circle approximation ($R=3440.065$ NM, $\pm 0.35\%$ error vs WGS-84).<br>2. Define a clean `GeodesicCalculator` interface isolating spherical from future WGS-84 ellipsoidal (Karney/Vincenty) implementations.<br>3. Separate Course Over Ground (COG) from Gyro/True Heading in telemetry data model and AIS parser (`aisMsg.PositionReport.TrueHeading` vs `Cog`).<br>4. Unit test shortest-arc modular delta: $359^\circ \to 1^\circ$ ($+2^\circ$), $1^\circ \to 359^\circ$ ($-2^\circ$), $0^\circ \to 180^\circ$ ($+180^\circ$), $180^\circ \to 0^\circ$ ($-180^\circ$). | `geo_test.go` verifying geodesic interface and shortest-arc delta edge cases across antimeridian. |
| **2** | **Running Standardized Residual (Z-Score)** | `server/internal/intelligence/state.go` | Replace incorrect division-by-mean $\frac{x-\mu}{\mu}$ with running sample moments:<br>$\mu_t = \alpha x_t + (1-\alpha)\mu_{t-1}$<br>$\sigma^2_t = \alpha(x_t - \mu_t)^2 + (1-\alpha)\sigma^2_{t-1}$<br>$Z_t = \frac{x_t - \mu_t}{\sqrt{\sigma^2_t + \epsilon}}$<br>Handle steady-state $\sigma \to 0$, sudden stop, speed/heading spikes, cold-start. | `state_test.go` covering zero variance, steady cruise, abrupt stops, speed spikes, missing reports. |
| **3** | **Circular Directional Statistics** | `server/internal/intelligence/state.go` | For absolute heading smoothing, use 2D unit vector trigonometric decomposition:<br>$\bar{C}_t = \alpha \cos\theta_t + (1-\alpha)\bar{C}_{t-1}$, $\bar{S}_t = \alpha \sin\theta_t + (1-\alpha)\bar{S}_{t-1}$, $\bar{\theta}_t = \text{atan2}(\bar{S}_t, \bar{C}_t)$.<br>For shortest-arc rate-of-turn deltas $|\Delta\theta|$, document why scalar EWMA is mathematically appropriate. | `circular_test.go` verifying mean heading across branch cut $359^\circ \leftrightarrow 1^\circ$. |
| **4** | **AIS Threat Semantics** | `server/internal/anomaly/scorer.go`, `server/internal/geo/landmask.go`, `pipeline.go` | 1. Reclassify AIS gap $>30$ min as `ais_continuity_anomaly` (dark-period indicator), noting non-hostile causes (VHF shadow, SOTDMA slot congestion).<br>2. Reclassify land-mask intersection as `position_integrity_anomaly` (chart datum offset / multipath / GPS degradation), not confirmed GPS spoofing.<br>3. Frame kinematic anomalies as probabilistic risk indicators rather than direct proof of hostile intent. | `anomaly_test.go` asserting updated reasons, taxonomy, and severity scoring. |
| **5** | **ML Pipeline Terminology** | `service/ml-service/lib/scoring.py`, `service/ml-service/lib/training.py` | Correct architectural documentation and code comments: designate Isolation Forest + LOF with static weighting as **weighted score blending**, not model stacking. | Code review & doc verification in `scoring.py` and `training.py`. |
| **6** | **ML Dataset Discipline & Manifest** | `service/ml-service/lib/training.py`, `api/train_all_models.py`, `models/manifest.json` | 1. Explicitly label parametric data as **synthetic benchmark dataset**.<br>2. Implement reproducible dataset pipeline with deterministic seed, train / validation / calibration / test splits.<br>3. Support group-based splitting by vessel MMSI (`GroupKFold`) and out-of-time temporal ordering.<br>4. Generate a comprehensive dataset `manifest.json` with hash lineage, feature schema, class balance, split rules. | ML test verifying split isolation, zero vessel leakage, and manifest generation. |
| **7** | **ML Calibration Architecture** | `service/ml-service/lib/training.py`, `service/ml-service/lib/scoring.py` | 1. Fit Isotonic Calibrator strictly on a separate held-out calibration set (never on test set).<br>2. Measure Expected Calibration Error (ECE) and Brier score.<br>3. Generate reliability diagram metrics. | `test_calibration.py` asserting Brier score and ECE calculation on test set. |
| **8** | **ML Metrics Suite** | `service/ml-service/lib/training.py`, `service/ml-service/analysis.py` | Compute and report complete metrics without pre-determined cherry-picking: Precision, Recall, F1, Specificity, Balanced Accuracy, ROC-AUC, PR-AUC, Brier Score, ECE, Confusion Matrix. | Automated benchmark script capturing exact confusion matrix and metric output. |
| **9** | **SHAP Explainability Scope** | `service/ml-service/lib/scoring.py`, `grpc_server.py`, `server/internal/intelligence/ml_client.go` | 1. Expose SHAP explainer scope explicitly as `isolation_forest` feature attribution.<br>2. Refrain from claiming SHAP explains final calibrated probability or LOF density.<br>3. Update response schemas to communicate explanation scope. | Contract test checking explanation metadata and scope attribution. |
| **10** | **ML Latency Instrumentation** | `service/ml-service/lib/scoring.py`, `grpc_server.py`, `service/sre/main.go` | Instrument every pipeline stage separately: Feature parsing, Scaling, IsolationForest, LOF, Isotonic Calibration, SHAP attribution, Serialization. Report p50, p95, p99 with and without SHAP. | SRE benchmark capturing sub-millisecond breakdown across CPU hardware. |
| **11** | **Go Queue Terminology & Metrics** | `server/internal/intelligence/pipeline.go`, `server/internal/observability/metrics.go` | 1. Reclassify queue as **bounded buffered Go channel with drop-tail backpressure**.<br>2. Expose `queue_depth`, `queue_capacity`, `queue_dropped_total`, `queue_processed_total`, `queue_wait_ms`. | `pipeline_test.go` checking atomic metrics and bounded channel queue semantics. |
| **12** | **Backpressure & Overload** | `server/internal/intelligence/pipeline.go` | Implement explicit overload shedding policies and verify system stability under downstream degradation. | Concurrency and burst overload unit test in `pipeline_test.go`. |
| **13** | **Circuit Breaker Hardening** | `server/internal/intelligence/ml_client.go` | 1. Retain 3-state Circuit Breaker with exponential backoff and jitter.<br>2. Emit Prometheus metrics and structured SRE alert logs on trip.<br>3. Ensure operator visibility during fallback. | `ml_client_test.go` simulating ML outage, recovery, and fallback alerting. |
| **14** | **Database Ingestion Architecture** | `server/internal/db/telemetry.go`, `server/internal/db/db.go` | 1. Correct RCA: B-Tree index lock contention and round-trip overhead vs PostgreSQL Group Commit.<br>2. Benchmark single-row INSERT vs multi-row batch INSERT vs COPY.<br>3. Document TimescaleDB hypertable chunk pruning semantics. | Database micro-benchmark in `db_test.go` measuring throughput across batch sizes. |
| **15** | **Connection Pooling Discipline** | `server/internal/db/db.go` | Validate PgBouncer transaction pooling mode constraints: enforce `prefer_simple_protocol=true` / `QueryExecModeSimpleProtocol`, document prepared statement / advisory lock limitations. | `db_test.go` verifying simple protocol execution under connection pooling. |
| **16** | **Python Concurrency & GIL Profiling** | `service/ml-service/grpc_server.py`, `app.py` | Measure CPU, serialization, feature parsing, and model execution times; benchmark single-process vs multi-process gRPC workers. | Multi-worker benchmark measuring gRPC RPS and latency. |
| **17** | **Modern gRPC Load Balancing** | `server/internal/intelligence/ml_client.go` | Remove deprecated `grpc.WithRoundRobin()` / `grpc.WithBlock()`; configure modern `grpc.WithDefaultServiceConfig` JSON round-robin load balancer. | `ml_client_test.go` verifying non-deprecated gRPC dial configuration. |
| **18** | **WebSocket Concurrency Model** | `server/internal/websocket/hub/hub.go`, `service/sre/main.go` | 1. Reclassify 1M WebSocket target as **Architectural Capacity Target**.<br>2. Build a local concurrent WebSocket load-test harness (1k–10k clients) measuring latency and memory. | SRE WebSocket load test verifying active connections and throughput. |
| **19** | **WebSocket Slow-Client Protection** | `server/internal/websocket/hub/hub.go` | Implement per-client bounded send channels, write deadlines (10s), non-blocking message dropping, and slow consumer eviction. | `hub_test.go` simulating slow and stalling clients without blocking fast peers. |
| **20** | **Network Capacity Model** | `docs/ARCHITECTURE.md`, `service/sre/capacity.go` | Correct the $1000\times$ arithmetic error ($8\text{ Tbps} \to 8\text{ Gbps}$); implement parameterized formula ($U, V, F, B, R$) yielding $720\text{ Mbps}$ cluster egress. | Mathematical validation test verifying capacity formulas and boundary conditions. |
| **21** | **NATS Core vs JetStream** | `docs/ARCHITECTURE.md` | Delineate in-memory Core NATS ($15\text{–}20\text{M msg/s}$) from persistent Raft JetStream ($50\text{k–}250\text{k msg/s}$ on NVMe). | Documentation & architecture audit update. |
| **22** | **Spatial Indexing Responsibilities** | `docs/ARCHITECTURE.md`, `server/internal/heatmap/aggregator.go` | Clearly partition spatial layers: H3 for broker topic routing, R-Tree for edge gateway viewports, PostGIS for spatial SQL queries. | Architectural specification review. |
| **23** | **SRE Instrumentation & Tracing** | `server/internal/observability/metrics.go`, `logging.go` | Add Prometheus counters and histograms for request rates, error rates, queue depths, ML latency, and W3C trace correlation IDs. | Observability test verifying Prometheus scraping output. |
| **24** | **Decoupled Health Probes** | `server/internal/api/handlers.go` | Separate startup, liveness (`/health/live` - process alive), and readiness (`/health/ready` - dependencies ready). Ensure liveness does not ping DB. | HTTP health probe tests in `handlers_test.go`. |
| **25** | **Automated Resilience & Chaos Suite** | `service/sre/main.go`, `service/sre/sre.sh` | Implement automated failure test commands for ML outage, DB failure, ingestion burst, and malformed inputs. | Automated chaos suite reporting detection, containment, and recovery times. |
| **26** | **Security Controls Remediation** | `server/internal/bootstrap/router.go`, `server/internal/api/middleware.go` | Implement token bucket rate limiting, pre-handshake WebSocket JWT validation, and document boundary controls. | Security middleware unit tests. |

---

## 3. Execution Schedule

1. **Phase 1: Code Remediation** (Execute all code changes across Go backend and Python ML service)
2. **Phase 2: Comprehensive Test Suite** (Add unit, integration, contract, ML, geospatial, concurrency, and resilience tests)
3. **Phase 3: Benchmark Suite Execution** (Run reproducible benchmarks for ML, DB, gRPC, WebSocket, and Chaos)
4. **Phase 4: Second Adversarial Audit** (Re-evaluate all claims C-01 to C-26 against measured numbers)
5. **Phase 5: Final Evidence Matrix** (Demonstrated vs Locally Validated vs Capacity Target vs Unsupported)
6. **Phase 6: Final Engineering Report** (Generate comprehensive 24-section production report)

# HormuzWatch ML Inference — Render Deployment
# Runtime dependencies only (no training deps, no pandas)
# Dev/training extras: pip install pandas

scikit-learn==1.5.0
numpy==1.26.4
pydantic==2.7.2
joblib==1.4.2
shap==0.45.1

# Added for Render Deployment
fastapi==0.111.0
uvicorn==0.30.1

# Added for OpenTelemetry Observability
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-exporter-otlp==1.25.0

# ─── Dev / test only (not deployed) ───────────────────────────────
# pytest==8.2.2
# pandas==2.2.2     # Required by api/train.py — not deployed

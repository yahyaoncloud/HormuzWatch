services:
  - type: web
    name: hormuzwatch-inference
    env: python
    buildCommand: "cd ml-inference && pip install -r requirements.txt"
    startCommand: "cd ml-inference && uvicorn app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

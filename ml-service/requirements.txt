# Unified HormuzWatch ML Service (merged ml-service + ml-inference)
# Runtime dependencies for both the legacy IsolationForest+SHAP model
# and the multi-domain ensemble (IsolationForest + LOF + isotonic).

fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.2
scikit-learn==1.5.0
numpy==1.26.4
joblib==1.4.2
shap==0.45.1

# gRPC transport (production: Go backend -> ml-service)
grpcio==1.65.1

# Conflict prediction pipeline
xgboost>=2.0.0
requests>=2.31.0
pandas>=2.2.0

# Dev / offline training / tests (not required at runtime):
#   pip install pandas pytest
# grpcio-tools==1.65.1   # required to regenerate ml_service_pb2*.py
# pandas==2.2.2     # required by api/train.py
# pytest==8.2.2       # required by tests/test_scoring.py
